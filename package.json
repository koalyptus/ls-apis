{
  "name": "ls-apis",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/aggregator",
    "packages/cli"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}